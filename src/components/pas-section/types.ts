import { LucideIcon } from 'lucide-react';

export interface AgitationPoint {
  emoji: string;
  title: string;
  description: string;
  icon: LucideIcon;
}

export interface SolutionFeature {
  title: string;
  description: string;
  icon: LucideIcon;
}